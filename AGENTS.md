- The config JSON format is still under development. Don't hesitate to make
  backward-compatible changes to it. Avoid adding default values to config
  fields.
- Maintain clean, readable code. When there is an opportunity to improve code
  quality by refactoring, suggest this, even if not required for the current
  task.
